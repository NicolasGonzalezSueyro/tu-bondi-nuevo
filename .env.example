# String de conexion de tu cluster gratis de MongoDB Atlas.
# Si la dejas vacia, en local guarda la sesion en una carpeta (./auth_info) en vez de Mongo.
MONGODB_URI=
MONGODB_DB_NAME=tubondi_whatsapp

# Puerto local (Render lo pisa automaticamente con su propia variable PORT).
PORT=3000
